{% set cyberduck = salt['grains.filter_by']({
    'Windows': {
        'package': 'cyberduck',
        'installer': 'chocolatey.installed'
    },
    'MacOS': {
        'package': 'homebrew/cask/cyberduck',
        'installer': 'pkg.installed'
    },
}, merge=salt['pillar.get']('cyberduck:lookup')) %}
