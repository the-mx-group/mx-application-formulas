{% set filezilla = salt['grains.filter_by']({
    'Windows': {
        'package': 'filezilla',
        'installer': 'chocolatey.installed'
    },
    'MacOS': {
        'package': 'Caskroom/cask/filezilla',
        'installer': 'pkg.installed'
    },
    'Arch': {
        'package': 'filezilla',
        'installer': 'pkg.installed',
    },salt['pillar.get']('filezilla:lookup')) %}
