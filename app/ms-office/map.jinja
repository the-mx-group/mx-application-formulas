{% set microsoft-office = salt['grains.filter_by']({
    'Windows': {
        'package': 'microsoft-office-deployment',
        'installer': {
          'chocolatey.installed': [
            {'package_args': '/64bit /Product:O365ProPlusRetail'}
          ]
        }
    },
    'MacOS': {
        'package': 'homebrew/cask/microsoft-office',
        'installer': 'pkg.installed'
    },
    'Arch': {
        'package': 'none',
        'installer': 'test.nop',
    },
}, merge=salt['pillar.get']('microsoft-office:lookup')) %}
