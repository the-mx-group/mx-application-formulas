{% set vlc = salt['grains.filter_by']({
    'Windows': {
        'package': 'vlc',
        'installer': 'chocolatey.installed'
    },
    'MacOS': {
        'package': 'Caskroom/cask/vlc',
        'installer': 'pkg.installed'
    },
    'Arch': {
        'package': 'vlc',
        'installer': 'pkg.installed'
    },
}, merge=salt['pillar.get']('vlc:lookup')) %}
