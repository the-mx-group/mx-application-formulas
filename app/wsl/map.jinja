{% set config = salt['grains.filter_by']({
  'Windows': {
    'package': 'wsl',
    'installer': 'chocolatey.installed'
  },
  'MacOS': {
    'package': 'WSL does not exist for MacOS',
    'installer': 'test.nop'
  },
  'Arch': {
      'package': 'Linux is already linux',
      'installer': 'test.nop',
  },
}, merge=salt['pillar.get']('wsl:lookup')) %}
