{% set zaproxy = salt['grains.filter_by']({
    'Windows': {
        'package': 'zap',
        'installer': 'chocolatey.installed',
    },
    'MacOS': {
        'package': 'homebrew/cask/owasp-zap',
        'installer': 'pkg.installed',
    },
    'Arch': {
        'package': 'zaproxy',
        'installer': 'pkg.installed',
    },
}, merge=salt['pillar.get']('zaproxy:lookup')) %}
